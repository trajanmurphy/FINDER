function [Aeq, beq] = ChebSep3Constraint(Datas, parameters, methods)


M = parameters.transform.dimTransformedSpace;
P = parameters.data.numofgene;

targetmu = ones(M,1);
realmu = mean(Datas.B.Training, 2);

id = speye(M);


Aeq = krond(id, realmu(:)');
Aeq = [Aeq, sparse(M,1)];

end